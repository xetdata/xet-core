[package]
name = "retry_strategy"
version = "0.11.0"
edition = "2021"

[features]
strict = []

[dependencies]
tokio-retry = "0.3.0"

[dev-dependencies]
anyhow = "1"
tokio = { version = "1.0", features = ["macros", "rt-multi-thread", "sync"] }
