[package]
name = "retry_strategy"
version = "0.14.5"
edition = "2021"

[features]
strict = []

[dependencies]
pin-project = "1.1.5"
rand = "0.8.5"
getrandom = { version = "0.2.15", features = ["js"] }

[dev-dependencies]
anyhow = "1"
